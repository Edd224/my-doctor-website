import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Objednanie - Alergológ",
  description: "Objednajte sa na alergologické vyšetrenie v našej klinike.",
};

const AppointmentPage = () => {
  return (
    <div>
      {/* Obsah stránky */}
    </div>
  );
};

export default AppointmentPage;
